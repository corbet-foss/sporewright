{
  "name": "05_unknown_level_rejected",
  "description": "A seed whose floor names a tier not in `levels` is unknown-level. instantiate must reject it with `unknown-level`, identically on both cores.",
  "config": {
    "seeds": [
      {
        "floor": "nonexistent",
        "layer": "global",
        "scope": {},
        "option": "groq",
        "dim": "financial",
        "kind": "value",
        "value": 0.0
      }
    ],
    "layers": [
      "global",
      "source",
      "job"
    ]
  },
  "expected_error": "unknown-layer"
}
